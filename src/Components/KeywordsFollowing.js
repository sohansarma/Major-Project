import React from 'react';



const Card = ({ followKeyWord,user }) => {
	return (
			<div className = 'tc bg-light-green dib br2 pa3 ma2 grow bw2 shadow-5 w-10'>
			    user.keyword.map((_id => {
						followKeyWord
					}))
		  </div>
	);
}

export default Card;
